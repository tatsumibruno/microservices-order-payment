spring:
  application:
    name: payment
  cloud:
    stream:
      bindings:
        orders-created:
          binder: kafka
          group: payment
        orders-changes:
          binder: kafka
      kafka:
        binder:
          brokers: localhost
          defaultBrokerPort: 9092
          autoCreateTopics: true
        bindings:
          orders-created:
            group: payment
            consumer:
              autoCommitOffset: true
              enableDlq: true
          orders-changes:
            producer:
              partitioned: true
              partitionCount: 1
              transacted: true
